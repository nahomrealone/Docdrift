# DocDrift

Testing the DocDrift GitHub Action.

## Development

Run DocDrift locally with:

```bash
npm run develop
```

## createUser

`createUser` accepts an email address and a boolean that determines whether a
welcome email should be sent.
